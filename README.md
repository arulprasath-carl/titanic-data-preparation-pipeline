# titanic-data-preparation-pipeline
A modular Python pipeline that cleans, validates, encodes, and scales the Titanic dataset for machine learning — includes stratified train-test splitting with no data leakage. Data preparation only, no model training.
